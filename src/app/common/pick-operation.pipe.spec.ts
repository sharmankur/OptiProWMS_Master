import { PickOperationPipe } from './pick-operation.pipe';

describe('PickOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new PickOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
