import { PickListStatusPipe } from './pick-list-status.pipe';

describe('PickListStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new PickListStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
