import { DatestringToobjectPipe } from './datestring-toobject.pipe';

describe('DatestringToobjectPipe', () => {
  it('create an instance', () => {
    const pipe = new DatestringToobjectPipe();
    expect(pipe).toBeTruthy();
  });
});
