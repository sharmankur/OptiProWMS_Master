import { TransferMeansPipe } from './transfer-means.pipe';

describe('TransferMeansPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferMeansPipe();
    expect(pipe).toBeTruthy();
  });
});
